import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    zIndex: 2,
    borderTopWidth: 1,
    borderTopColor: '#333',
    width: '100%',
    height: 40,
  },
});
